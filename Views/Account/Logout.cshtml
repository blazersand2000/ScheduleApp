@model ScheduleApp.Models.LoginViewModel

@{
   ViewData["Title"] = "Logout Successful";
}

<div class="jumbotron jumbotron-fluid">
   <div class="container">
      <h1 class="display-4">Logout Successful</h1>
      <p class="lead"><a asp-action="Login">Click here</a> to log in again.</p>
   </div>
</div>

@section Scripts {
   @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
